import { React } from "react";
import "../Styles/AdminDashboard.css";

export default function Dashboard() {
    return(
        <div className = "admindashboard-container">
            <p className = "admindashboard-container-header">Dashboard</p>
        </div>
    )
}
import { React } from "react";
import "../Styles/StudentHistory.css";

export default function History() {
    return(
        <div className = "studenthistory-container">
            <p className = "studenthistory-container-header">History</p>
        </div>
    )
}